query getReleases($input: SearchInput!) {
    searchReleases(input: $input) {
        total
        page
        pages
        results {
            title
            country
            year
            format
        }
    }
}